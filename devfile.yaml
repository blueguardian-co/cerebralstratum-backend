schemaVersion: 2.1.0
metadata:
  name: python-hello-world
  namespace: che-kube-admin-devspaces-6skydy
projects:
  - name: cs-backend
    git:
      remotes:
        origin: git@github.com:blueguardian-co/cerebralstratum-backend.git
      checkoutFrom:
        revision: main
components:
  container:
    - command:
      - /checode/entrypoint-volume.sh
      cpuLimit: 500m
      cpuRequest: 30m
      env:
        - name: CHE_DASHBOARD_URL
          value: 'https://devspaces.apps.k8s.idm.hussdogg.com'
        - name: CHE_PLUGIN_REGISTRY_URL
          value: 'https://devspaces.apps.k8s.idm.hussdogg.com/plugin-registry/v3'
        - name: CHE_PLUGIN_REGISTRY_INTERNAL_URL
          value: 'http://plugin-registry.openshift-operators.svc:8080/v3'
        - name: OPENVSX_REGISTRY_URL
          value: 'https://open-vsx.org'
      image: 'registry.redhat.io/devspaces/udi-rhel8:latest'
      memoryLimit: 3.07G
      memoryRequest: 256Mi
      endpoints:
        - exposure: public
          name: csapi
          protocol: http
          targetPort: 6443
      mountSources: true
      sourceMapping: /projects
      volumeMounts:
        - name: venv
          path: /home/user/venv
        - name: checode
          path: /checode
    name: python
  - endpoints:
      - exposure: public
        name: cswa
        protocol: http
        targetPort: 3000
    image: quay.io/ahussey-redhat/cs-wa:stable
    name: cswa
  - name: venv
    volume:
      size: 1G
commands:
  - exec:
      commandLine: python -m venv venv && source ./venv/bin/activate && cs-api
      component: python
      group:
        kind: run
      label: Run the application
      workingDir: '${PROJECTS_ROOT}'
    id: run
