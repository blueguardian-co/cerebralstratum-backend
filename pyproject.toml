[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cs-api"
version = "0.0.1"
dependencies = [
    "flask",
    "black"
]

[tool.setuptools.packages.find]
exclude = ["test", "tests.*"]

[project.scripts]
cs-api = "flaskr.__main__:main"
