# CEREBRAL STRATUM - Backend

## Setup dev environment

```bash
# Create and activate a virtual environment to dev in
python3 -m venv venv
source venv/bin/activate
# Install the application in editable mode
python3 -m pip install -e .
```

## How do?

```bash
# Start the API backend using the Werkzeug development server
cs-api
```
