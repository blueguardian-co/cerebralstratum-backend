#!/usr/bin/env python3

"""
Database API classes and mapping
"""

class subscriber():
    """
    A class that defines the `subscriber` table, which contains all subscribers
    and relevant details.
    """

class devices():
    """
    A class that defines the `device` table, which contains all relevant details
    about each device.
    """

class device():
    """
    A class the defines a table for each `device`
    Makes use of Postgres GIS for lat/long storage
    """